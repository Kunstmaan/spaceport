version: '2'
options:
  #compose-file-path: './docker-compose.yml'
  compose-dev-file-path: './docker-compose-mac.yml'
  verbose: false
syncs:
    {{ shuttle.name }}-app-sync:
        src: './'
        sync_excludes: ['.sass-cache/', 'sass/', 'sass-cache/', '.git/', 'var/cache/', 'app/cache', 'web/uploads/']
