version: '2'
options:
  compose-dev-file-path: './docker-compose-mac.yml'
  verbose: false
syncs:
    {{ shuttle.name }}-app-sync:
        src: './'
        sync_excludes: ['Path src', '.sass-cache', 'sass', '.git', 'var/cache', 'app/cache', 'web/uploads', '.docker-sync', '.idea']
