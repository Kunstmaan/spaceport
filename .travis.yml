language: php
sudo: false
cache:
  directories:
    - $HOME/.composer/cache/files
matrix:
  fast_finish: true
  include:
    - php: 8.0
before_install:
  - mkdir out
  - composer self-update
script:
  - composer install --optimize-autoloader
